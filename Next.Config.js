/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["images.unsplash.com"], // yaha apni product images ka domain add karna
  },
};

module.exports = nextConfig;
